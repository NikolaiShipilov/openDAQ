//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     RTGen (PythonGenerator).
// </auto-generated>
//------------------------------------------------------------------------------

/*
 * Copyright 2022-2025 openDAQ d.o.o.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

#include <pybind11/gil.h>

#include "py_opendaq/py_opendaq.h"
#include "py_core_types/py_converter.h"
#include "py_core_objects/py_variant_extractor.h"

PyDaqIntf<daq::ICredentialDescriptor, daq::IBaseObject> declareICredentialDescriptor(pybind11::module_ m)
{
    py::enum_<daq::CredentialFormat>(m, "CredentialFormat")
        .value("None_", daq::CredentialFormat::None)
        .value("KeyValuePairs", daq::CredentialFormat::KeyValuePairs)
        .value("String", daq::CredentialFormat::String)
        .value("FilePath", daq::CredentialFormat::FilePath);

    return wrapInterface<daq::ICredentialDescriptor, daq::IBaseObject>(m, "ICredentialDescriptor");
}

void defineICredentialDescriptor(pybind11::module_ m, PyDaqIntf<daq::ICredentialDescriptor, daq::IBaseObject> cls)
{
    cls.doc() = "Describes the shape of the secret(s) required by an authentication method used by the module and produced by a credential provider.";

    m.def("KeyValueDescriptor", [](std::variant<daq::IString*, py::str, daq::IEvalValue*>& id, std::variant<daq::IDict*, py::dict>& keys, std::variant<daq::IString*, py::str, daq::IEvalValue*>& description, daq::ITypeManager* typeManager, std::variant<daq::IString*, py::str, daq::IEvalValue*>& secretClassName){
        return daq::KeyValueDescriptor_Create(getVariantValue<daq::IString*>(id), getVariantValue<daq::IDict*>(keys), getVariantValue<daq::IString*>(description), typeManager, getVariantValue<daq::IString*>(secretClassName));
    }, py::arg("id"), py::arg("keys"), py::arg("description"), py::arg("type_manager"), py::arg("secret_class_name"));

    m.def("StringDescriptor", [](std::variant<daq::IString*, py::str, daq::IEvalValue*>& id, std::variant<daq::IString*, py::str, daq::IEvalValue*>& description, const bool hidden, daq::ITypeManager* typeManager, std::variant<daq::IString*, py::str, daq::IEvalValue*>& secretClassName){
        return daq::StringDescriptor_Create(getVariantValue<daq::IString*>(id), getVariantValue<daq::IString*>(description), hidden, typeManager, getVariantValue<daq::IString*>(secretClassName));
    }, py::arg("id"), py::arg("description"), py::arg("hidden"), py::arg("type_manager"), py::arg("secret_class_name"));

    m.def("FilePathDescriptor", [](std::variant<daq::IString*, py::str, daq::IEvalValue*>& id, std::variant<daq::IString*, py::str, daq::IEvalValue*>& description, daq::ITypeManager* typeManager, std::variant<daq::IString*, py::str, daq::IEvalValue*>& secretClassName){
        return daq::FilePathDescriptor_Create(getVariantValue<daq::IString*>(id), getVariantValue<daq::IString*>(description), typeManager, getVariantValue<daq::IString*>(secretClassName));
    }, py::arg("id"), py::arg("description"), py::arg("type_manager"), py::arg("secret_class_name"));

    m.def("NoneDescriptor", [](std::variant<daq::IString*, py::str, daq::IEvalValue*>& id, std::variant<daq::IString*, py::str, daq::IEvalValue*>& description, daq::ITypeManager* typeManager){
        return daq::NoneDescriptor_Create(getVariantValue<daq::IString*>(id), getVariantValue<daq::IString*>(description), typeManager);
    }, py::arg("id"), py::arg("description"), py::arg("type_manager"));


    cls.def_property_readonly("authentication_method_id",
        [](daq::ICredentialDescriptor *object)
        {
            py::gil_scoped_release release;
            const auto objectPtr = daq::CredentialDescriptorPtr::Borrow(object);
            return objectPtr.getAuthenticationMethodId().toStdString();
        },
        "Gets the id that uniquely identifies the authentication method this descriptor belongs to, at least within the module that offers it. In practice often unique system-wide instead: the Standard*CredentialDescriptor factories key off shared, well-known ids resolved through the one ITypeManager shared by the whole Context, so different modules using the same standard id produce identically-shaped descriptors.");
    cls.def_property_readonly("format",
        [](daq::ICredentialDescriptor *object)
        {
            py::gil_scoped_release release;
            const auto objectPtr = daq::CredentialDescriptorPtr::Borrow(object);
            return objectPtr.getFormat();
        },
        "Gets the format of the described secret(s).");
    cls.def_property_readonly("parameters",
        [](daq::ICredentialDescriptor *object)
        {
            py::gil_scoped_release release;
            const auto objectPtr = daq::CredentialDescriptorPtr::Borrow(object);
            return objectPtr.getParameters().detach();
        },
        py::return_value_policy::take_ownership,
        "Gets the format's standard parameter set, as a Struct - see the class description for which formats have one and what it carries. Unassigned if the format has none.");
    cls.def_property_readonly("description",
        [](daq::ICredentialDescriptor *object)
        {
            py::gil_scoped_release release;
            const auto objectPtr = daq::CredentialDescriptorPtr::Borrow(object);
            return objectPtr.getDescription().toStdString();
        },
        "Gets the description of the authentication method, for the user. States how the module interpretes it, e.g. \"PIN-code\", \"username and password\", \"Path to file containing the SSH private key\".");
    cls.def("create_empty_secret",
        [](daq::ICredentialDescriptor *object)
        {
            py::gil_scoped_release release;
            const auto objectPtr = daq::CredentialDescriptorPtr::Borrow(object);
            return objectPtr.createEmptySecret().detach();
        },
        "Builds an empty secret matching this descriptor's shape - a property object with one empty (default `\"\"`) String property per secret value the format expects: for `KeyValuePairs`, one property per key named in `getParameters()`'s `\"Keys\"` dict (e.g. `\"UserName\"`, `\"Password\"`); for `String` and `FilePath`, a single property, named and described by the descriptor's own registered secret class. Not supported for `None` - a `None`-format authentication method requires no credentials at all, so no secret is ever needed for it in the first place; therefore raises.");
}
